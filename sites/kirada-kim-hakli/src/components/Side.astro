---
import { sideShort, type Side } from "../lib/cases";
interface Props { v: Side; lg?: boolean }
const { v, lg = false } = Astro.props;
---
<span class={`tag tag-${v}${lg ? " tag-lg" : ""}`}>{sideShort[v]}</span>
